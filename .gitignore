.DS_Store
.codex/
.build/
.swiftpm/
dist/
DerivedData/
*.xcuserstate
*.log

# Grok Build is an external runtime dependency and is never vendored here.
grok-build/

# Local credentials and runtime state must never enter the repository.
auth.json
state.json
sessions/
memory/

# Locally staged installers / cross-compiled binaries (CI publishes these).
desktop/packaging/**/*.dmg
desktop/packaging/**/*.exe
desktop/packaging/**/grokdesk.exe
desktop/src/vscodeIcons.json
desktop/src-tauri/target/
